syntax = "proto3";
option go_package = "dummy/";

// this file defines the proto definitions of the messages that are used in dummy replica

message Ping {
  int32 id = 1;
}

message Pong {
  int32 id = 1;
}